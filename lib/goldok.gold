# vim: ft=awk ts=2 sw=2 et :
# unit tests
function okFail(f) { ok(f,1==2) }

function okFail(f) { ok(f,1==1) }

function okSym(f, s) {
  Sym(s)
  oo(s)
}

function okSyms(f, s,a,tmp) {
  split("aaaaabbc",a,"")
  adds(a,tmp,"Sym")
  assert(1.378 <= var(tmp) && var(tmp) <= 1.379)
  #s=adds({"a","a","a","a","a","a","a"},Sym.new())
  #assert(s:ent()==0)
}

